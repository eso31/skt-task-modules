spring:
  application:
    name: config-client
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888
      username: ${CONFIG_SERVER_USER}
      password: ${CONFIG_SERVER_PASSWORD}
