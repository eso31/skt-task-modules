server:
  port: 8080

spring:

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

  rabbitmq:
    host: localhost
    port: 3131
    username: ${RABBITMQ_USER}
    password: ${RABBITMQ_PASSWORD}